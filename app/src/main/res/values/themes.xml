<resources>
    <!--
        Compose owns all in-app colour; this theme only needs to give the window
        a sane background before the first frame and drop the action bar.
    -->
    <style name="Theme.QsActions" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
